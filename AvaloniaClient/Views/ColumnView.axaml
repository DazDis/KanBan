<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="AvaloniaClient.ColumnView">
  <!--<ScrollViewer HorizontalScrollBarVisibility="Auto">
    <ItemsControl ItemsSource="{Binding Columns}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch"/>
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Border Width="300" Margin="10" Background="LightGray">
                    <StackPanel>
                        <Grid>
                            <TextBlock Text="{Binding Title}" FontWeight="Bold"/>
                            <Button Command="{Binding RemoveColumnCommand}" 
                                    CommandParameter="{Binding}" 
                                    Content="X" HorizontalAlignment="Right"/>
                        </Grid>
                        
                        <ListBox ItemsSource="{Binding Tasks}" AllowDrop="True">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="White" Margin="2" Padding="5">
                                        <TextBlock Text="{Binding Title}"/>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        
                        <Button Content="+" Command="{Binding AddTaskCommand}"/>
                    </StackPanel>
                </Border>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</ScrollViewer>-->
</UserControl>
